# Log replication with multi-paxos

Project carried out in the school context, you can find all the explanations and details about the multi-paxos in [the report](rapport-ALGOREP.pdf)

# Prerequisites

*jq* must be installed.

to install required python package :

```shell
$ make env
```

# Exe©cution

```shell
$ make config=<configfile>
```

# Report

*pandoc* must be installed.

```shell
$ make pdf
```

# Tests

```shell
$ make test 
```
